# .github/workflows/build-foulout-stats.yml

name: Build Foul-out Stats

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 350

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Run build-foulout-stats
        run: node scripts/build-foulout-stats.js

      - name: Final push (safety net)
        if: always()
        run: |
          git add players/ scripts/.foulout-progress.json || true
          git diff --staged --quiet || git commit -m "build-foulout-stats: safety-net final commit"
          git pull --rebase=false --no-edit -X ours || true
          git push || true
