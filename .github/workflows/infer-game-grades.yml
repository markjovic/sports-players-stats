# .github/workflows/infer-game-grades.yml

name: Infer Game Grades (Local)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  infer:
    runs-on: ubuntu-latest
    timeout-minutes: 350

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Run infer-game-grades
        run: node scripts/infer-game-grades.js

      - name: Final push (safety net)
        if: always()
        run: |
          git add games/bv/ || true
          git diff --staged --quiet || git commit -m "infer-game-grades: safety-net final commit"
          git pull --rebase=false --no-edit -X ours || true
          git push || true
