# .github/workflows/lookup-grade-players.yml
name: Grade Player Lookup

on:
  workflow_dispatch:
    inputs:
      season_id:
        description: 'PlayHQ competition season ID to search (default: 0869ea69)'
        required: false
        default: '0869ea69'
      grade_id:
        description: 'PlayHQ grade ID for display context only (default: 5afff92b)'
        required: false
        default: '5afff92b'

jobs:
  lookup:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run grade lookup
        run: node lookup-grade-players.js --season=${{ github.event.inputs.season_id }} --grade=${{ github.event.inputs.grade_id }}

      - name: Commit HTML report
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add grade-lookup-result.html
          git diff --cached --quiet || git commit -m "Grade lookup: ${{ github.event.inputs.season_id }} [$(date -u '+%Y-%m-%d %H:%M')] [skip ci]"
          git pull --rebase origin main
          git push
